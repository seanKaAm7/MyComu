# 프로젝트 시스템 지침

이 파일의 규칙은 이 프로젝트에서 작업하는 모든 AI 작업자에게 적용한다.

## 1. 언어 사용 규칙

- 사용자 답변, 질문, 작업 계획, 작업 목록, 진행 상황, 구현 설명, 검증 결과를 모두 한국어로 작성한다.
- `implementation_plan.md`, `walkthrough.md`, `task.md`를 포함한 모든 문서 산출물을 한국어로 작성한다.
- 코드 식별자, 라이브러리 이름, 명령어처럼 번역하면 의미가 달라지는 항목만 원문 표기를 허용한다.
- 이모티콘은 사용하지 않는다.

## 2. 사용자 로그 관리

- 로그 파일은 프로젝트 루트의 `user_log.js`만 사용한다.
- 프로젝트 폴더가 없으면 로그를 작성하지 않는다.
- `user_log.js`는 추가 전용 파일이다. 기존 로그를 수정하거나 삭제하거나 순서를 바꾸지 않는다.
- 구현, 수정, 리팩터링, 리뷰, 테스트, 빌드, 패키징, 릴리스, 프로젝트 설정 변경처럼 프로그램 개발에 직접 필요한 사용자 요청만 새 로그로 추가한다.
- 공부용 개념 질문, 코드나 도구에 대한 단순 설명 요청, 일상 대화, 프로그램 개발과 무관한 질문은 로그에 추가하지 않는다.
- 하나의 요청에 개발 작업과 비개발 질문이 함께 있으면 개발 요청으로 보고 프롬프트 원문 전체를 기록한다.
- 로그의 `사용자` 항목에는 사용자의 프롬프트 원문을 생략하거나 요약하지 않고 100% 기록한다.
- 로그의 `작업` 항목에는 해당 요청에서 실제로 수행한 작업을 한국어로 상세히 요약한다. 작업 전 기록할 때는 수행 예정 내용을 적고, 작업 완료 후 같은 번호를 수정하지 않는다.
- 각 로그는 이전 마지막 번호에 1을 더한 일련번호를 사용한다.
- 날짜와 시간은 현재 프로젝트 환경의 현지 시간을 `년-월-일 시:분:초` 형식으로 기록한다.
- JavaScript 문법이 깨지지 않도록 로그 전체를 주석으로 기록한다. 사용자 원문에 `*/`가 포함될 수 있으므로 줄 주석 형식을 우선 사용한다.

로그 형식:

```javascript
// -----------------------------------------
// [Log #번호] [년-월-일 시:분:초]
//  * 사용자: 사용자 프롬프트 원문
//  * 작업: 수행 작업 상세 요약
// -----------------------------------------
```

### 로그 추가 명령

- PowerShell을 사용할 수 있는 환경에서는 반드시 다음 형식으로 원자적 추가를 수행한다.

```powershell
Add-Content -Path "./user_log.js" -Value "추가할 로그" -Encoding utf8
```

- PowerShell이 없는 환경에서는 프로젝트가 제공하는 파일 편집 도구로 파일 마지막에만 추가한다. 이때 기존 내용을 패치 문맥으로 읽을 수는 있지만 기존 로그 행은 한 글자도 변경하지 않는다.
- 파일 전체를 다시 생성하는 방식, 덮어쓰기 리다이렉션, 정렬, 포맷터 적용을 금지한다.

## 3. 패치 노트 관리

- 주요 기능 추가, 사용자에게 보이는 동작 변경, 구조 변경이 있을 때만 프로젝트 루트의 `PATCH_NOTES.js`를 갱신한다.
- 새 패치 노트는 항상 기존 내용의 맨 위에 추가한다.
- 사소한 오타 수정, 조사, 단순 질의응답은 패치 노트에 기록하지 않는다.
- 패치 노트는 다음 형식을 사용한다.

```javascript
/*
---------
[년 월 일]
  * 변경 사항 1
  * 변경 사항 2
---------
*/
```

## 4. 작업 시작과 로그 연속성

모든 사용자 요청에서 먼저 이 `AGENTS.md`를 다시 확인하고 로그 대상인지 판단한다.
로그 대상이 아닌 공부용·비개발 요청은 `user_log.js`를 확인하거나 수정하지 않고 바로 답변한다.
로그 대상인 개발 요청은 다음 순서를 지킨다.

1. 프로젝트 루트의 `user_log.js` 존재 여부를 확인한다.
2. PowerShell이 있으면 `Get-Content -Tail 20 "user_log.js"`로 마지막 로그를 확인한다.
3. PowerShell이 없으면 운영체제에서 사용 가능한 읽기 명령으로 마지막 20줄을 확인한다.
4. 마지막 로그가 20줄보다 길어 번호가 보이지 않으면 파일에서 마지막 `[Log #번호]` 표식을 추가로 검색한다.
5. 마지막 로그 번호에 1을 더한다. 파일이 없거나 로그가 없으면 `Log #1`부터 시작한다.
6. 현재 사용자 프롬프트 원문과 수행 예정 작업을 파일 끝에 즉시 추가한다.
7. 프로젝트 루트의 `체크리스트.md`를 읽고 이번 작업에 적용할 항목을 확인한다.
8. 그 다음에 조사, 구현, 수정, 실행, 답변을 진행한다.

## 5. 개발 품질 체크리스트 적용

- 프로젝트 루트의 `체크리스트.md`는 모든 향후 설계, 구현, 수정, 리뷰, 테스트에 적용되는 필수 기준이다.
- 새 기능을 만들기 전에 모듈 경계, 의존 방향, 공용 타입의 단일 출처, 실패 경로와 부작용을 먼저 확인한다.
- 새 함수, 헬퍼, 타입을 만들기 전에 같은 책임의 기존 구현이 있는지 검색한다.
- 에러를 조용히 삼키지 않으며 처리 책임은 IPC, 훅, 화면 오류 상태 같은 명확한 경계에 둔다.
- 중첩, 복잡도, 파일과 함수 크기 제한을 우회하지 않는다. 제한을 넘으면 책임을 분리한다.
- 테스트는 정상 경로뿐 아니라 빈 입력, 잘못된 값, 경계값, 실패 경로, 자원 정리와 전역 부작용을 포함한다.
- 구현 완료 후 기본적으로 `npm run check`를 실행한다. 빌드나 패키징에 영향을 주는 변경은 `npm run build` 또는 `npm run package`도 실행한다.
- ESLint나 테스트를 통과시키기 위해 규칙을 임의로 끄거나 의미 없는 방어 코드를 추가하지 않는다.
- 체크리스트와 충돌하는 구현이 불가피하면 적용 전에 이유와 범위를 계획 문서 또는 코드 근처에 명확히 기록한다.

## 6. 응답 직전 확인

최종 답변을 보내기 직전에 다음 사항을 확인한다.

1. 답변과 새 산출물이 한국어인지 확인한다.
2. 이모티콘이 없는지 확인한다.
3. 이번 요청이 로그 대상인 개발 요청이라면 사용자 프롬프트가 `user_log.js`에 원문 그대로 기록되었는지 확인한다.
4. 주요 변경이 있었다면 `PATCH_NOTES.js`의 맨 위에 패치 노트가 추가되었는지 확인한다.
5. 기존 로그가 수정되거나 삭제되지 않았는지 확인한다.
6. 코드 변경이 있었다면 `체크리스트.md`의 설계, 리뷰, 강제, 테스트 기준을 다시 확인하고 필요한 검증을 통과했는지 확인한다.
